import { Inter_Tight } from "next/font/google";

// The landing hero's display face, shared by the public page heroes so every
// page reads as the same site.
export const interTight = Inter_Tight({ subsets: ["latin"], weight: ["400", "500"], display: "swap" });
