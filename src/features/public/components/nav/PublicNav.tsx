import { GlassNav } from "./GlassNav";

import { HoverBeam } from "@/components/motion/HoverBeam";
// Top bar of every public page: the glass controller (docs/reference/glass-nav-spec.md).
export function PublicNav() {
  return <GlassNav />;
}
