// Drizzle schema — single source of truth for the database.
// Every table gets id, created_at, updated_at; money is integer cents;
// timestamps are timestamptz in UTC. Migrations only via `pnpm db:generate`.
//
// No tables yet.
export {};
